for (let i = 0; i < n; i + c) {
  // Some work
}

// Time Complexity is Θ(n/c) ==> Θ(n).

for (let i = 0; i < n; i - c) {
  // Some work
}

// Time Complexity is Θ(n/c) ==> Θ(n).

for (let i = 0; i < n; i * c) {
  // Some work
}

// Time Complexity is Θ(logcn).

for (let i = 0; i < n; i / c) {
  // Some work
}

// Time Complexity is Θ(logcn).

for (let i = 0; i < n; i ** c) {
  // Some work
}

// Time Complexity is Θ(logclog2n).
